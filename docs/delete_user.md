### DELETE /api/v1/users/{id}

**Beschreibung**: Löscht einen Nutzer mit dem angegebenen ID.

**Parameter**:
- `id` (integer): die ID des zu löschenden Nutzers

**Antwort**:
```json
{"message": "User deleted"}
```